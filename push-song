#!/bin/bash

cd $(dirname $0)

RSYNC_FLAGS="--times --checksum --human-readable --progress --recursive --compress"
DST_HOST=song
DST_DIR=/home/core/dissertation

rsync $RSYNC_FLAGS -L slides $DST_HOST:$DST_DIR/
